# CR-036 Banner/Ribbon Approval Packet

Captured on 2026-07-16 from the local React Router app at `http://127.0.0.1:5174/`.

## Banner Image Source

The current app shell implementation uses:

`/images/country-ranking-banner-placeholder.svg`

This source is defined as `bannerImageSrc` in `app/components/app-shell.tsx` and resolves to `public/images/country-ranking-banner-placeholder.svg`.

## Screenshots

- Desktop: `desktop.png` at 1440 x 1100
- Mobile: `mobile.png` at 390 x 900

Both screenshots render the app shell banner image and navigation ribbon.
